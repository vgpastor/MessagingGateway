// Re-export from core — this file exists for backwards compatibility
export type {
  ConnectionStatus,
  ConnectionInfo,
  ConnectionManagerPort,
} from '../../core/accounts/connection-manager.port.js';
